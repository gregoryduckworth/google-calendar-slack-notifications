{
  "name": "google-calendar-slack-notifications",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "axios": "^1.3.2",
    "dotenv": "^16.0.3",
    "googleapis": "^110.0.0",
    "jest": "^29.4.2",
    "serverless": "^3.27.0",
    "serverless-plugin-typescript": "^2.1.4",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "prepare": "cp -n .env.sample .env || true",
    "sls": "serverless",
    "sls:deploy": "sls -- deploy",
    "sls:local": "sls invoke local --function cronHandler",
    "sls:logs": "sls -- logs -f cronHandler",
    "test": "jest"
  }
}
